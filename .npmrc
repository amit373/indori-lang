# pnpm configuration
# This file ensures proper workspace linking and publishing behavior

# Enable strict peer dependencies (recommended for workspaces)
strict-peer-dependencies=false

# Auto install peers (useful for workspace development)
auto-install-peers=true

# Publishing configuration
# When publishing, pnpm will use npm registry
publish-branch=main
